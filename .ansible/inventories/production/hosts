all:
  children:

    manager:
      hosts:
        prod.diesel.net:

    drone:
      hosts:
        drone.diesel.net:

    tools:
      hosts:
        tools.diesel.net:

    quadro:
      hosts:
        quadro.diesel.net:

  vars:
    environment: production
    clean_install: no
    ansible_user: automation
    ansible_python_interpreter: /usr/bin/python3
